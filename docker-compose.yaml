version: '3'

services:
  qdrant_database:
    container_name: qdrant
    image: qdrant/qdrant
    ports:
      - 6333:6333
    networks:
      - default
#    volumes:
#      - qdrant-volume: /var/lib/docker/qdrant

  main_stream:
    image: llamaindex-project
    container_name: llamaindex-project



#volumes:
#  qdrant_volume:
